[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agileeye-detector"
version = "0.1.0"
description = "AgileEye local detector sidecar prototype"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["agileeye_detector*"]
